[constraints]
max_cycles = 0
max_cc = 20
# Legacy ceilings are dominated by inline host tests. The baseline gate catches
# smaller aggregate regressions while new code remains below these maxima.
max_file_lines = 2381
max_fn_lines = 133
no_god_files = true
max_upward_violations = 0
