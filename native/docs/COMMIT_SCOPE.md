# Commit scope

This branch adds the native Rust driver core, QSPI composition layer, shared-ring RPU engine, Embassy Ethernet adapter, tests, and CI. The release-specific Nordic `ControlCodec` and the nRF5340 board implementation remain explicit integration items. They are not replaced by test-only identifiers or guessed structures.
