# Static verification scope

A successful static run proves that the Rust source formats, compiles, passes unit tests, passes Clippy with warnings denied, and builds its core without default features.

A successful static run does not prove that undocumented or release-specific Nordic firmware behavior is correct. The physical-board acceptance plan remains mandatory.
