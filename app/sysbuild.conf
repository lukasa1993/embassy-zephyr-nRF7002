# nRF70 firmware is an external child image in NCS.  Partition Manager is not
# needed by this L2-only proof and would add unrelated flash layout inputs.
SB_CONFIG_WIFI_NRF70=y
SB_CONFIG_PARTITION_MANAGER=n
